{
  "name": "fashion-store-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web-store",
    "build": "npm run build -w apps/web-store"
  },
  "dependencies": {
    "@tabler/icons-react": "^3.41.1"
  }
}
